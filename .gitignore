/target

# Node dependencies (run npm install in frontend/ after clone)
node_modules/

# Fetched upstream (scripts/fetch_stfc_data.ps1)
data/upstream/

# User-specific import/sync output (generated in rosters/)
rosters/roster.imported.json

# Downloaded spreadsheets (e.g. Syndicate Progression)
data/import/*.xlsx

# Env files (secrets)
.env
.env.local

# Simulator benchmark log (periodic runs)
benchmark_log.csv

# Sync ingress log (append-only when POST /api/sync/ingress is received)
sync.log
