# Import logs

Structured logs from building (and future) import runs live here, e.g.:

- `buildings-stfcspace-YYYY-MM-DD.json`

Each log includes source, data_version, counts, and any unmapped labels or errors.
